{
  "name": "justai",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:web dev:server",
    "dev:web": "npm --prefix apps/web exec -- vite --port 4301",
    "dev:server": "npm --prefix server exec -- nodemon --watch src --exec ts-node-esm src/index.ts",
    "build": "npm --prefix apps/web run build && npm --prefix server run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
