{
  "name": "justai",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix apps/web",
    "install:all": "npm install && npm install --prefix server && npm install --prefix apps/web",
    "build": "npm run build:server && npm run build:web",
    "build:server": "npm run build --prefix server",
    "build:web": "npm run build --prefix apps/web"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "eslint": "^9.31.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  }
}
